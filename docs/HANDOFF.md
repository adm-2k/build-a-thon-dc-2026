# HANDOFF.md — Cross-lane queue

The only file every lane may append to — and it has **exactly one live copy**:
this file in **A's main checkout**, addressed by absolute path
(`~/Documents/GitHub/build-a-thon-dc-2026/docs/HANDOFF.md`). Lane sessions
read and append that path directly (the one sanctioned exception to "stay in
your worktree"); never the copy inside your own worktree, which is a stale
snapshot. Only A commits this file. Keep entries to one line each; the
orchestrator polls roughly every 15 minutes (docs/ORCHESTRATION.md §7). Never
put a key, token, or URL-with-secret in this file — name the env var you need
and the orchestrator delivers it out-of-band.

## ORCHESTRATOR QUEUE

Append-only. `TYPE` is one of `KEY` (need a credential/env var), `DECISION`
(need a call only A can make), `MERGE` (PR ready for review), `BLOCKED`
(blocked >10 min, moving to next charter item).

| Time | Lane | Type | Ask (one line) | Status |
|------|------|------|----------------|--------|
| —    | —    | —    | *(example: `13:40 · D · KEY · need SEARCH_API_KEY in my .env.local to record live search fixtures` — note: only lanes A and D ever file KEY rows; B/C/E are keyless by design)* | open |
| 00:58 | tracer | MERGE | PR #6 `[lane-tracer] Scriptorium page — image intake + transcription flow` — build/smoke/hex-grep/ownership green, evidence in PR body | done |
| 00:58 | tracer | DECISION | not blocking, FYI: Scriptorium's transcription/save calls degrade `/api/ocr` and `/api/documents` 404s to LACUNA today (those routes are Lane A v2 items 3–4 not yet on `main`); `OcrResult` is mirrored locally in `app/scriptorium/ocr-client.ts` verbatim from SPEC §3 and marked `LACUNA(lane-tracer)` — will swap to the real `lib/engine/schemas.ts` import the moment Lane A merges it, no ask needed | done |
| 01:17 | D | DECISION | reference corpus pick (charter item 1) — EN print: EB1911 Vol.22 p.916 "RATIONALISM" (upload.wikimedia.org, thumb of EB1911_-_Volume_22.djvu page933); DE Antiqua: "Die Kunst" Bd.4 (1899, IA `diekunstmonatshe04mnuoft`) leaf n20, running text on the 1900 Paris Buchgewerbe-Ausstellung; DE Fraktur: "Die Gartenlaube" (1899) p.880 (commons `Die_Gartenlaube_(1899)_0880.jpg`), start of "Ueber Nervenschutz und Nervenstärkung." All pre-1930, public domain in the US, verified by visual inspection (plain running text, not plates). Proceeding to produce transcriptions unless overruled. | done |
| 01:17 | D | BLOCKED | `fixtures/ocr/*` and `fixtures/ner/*` (charter item 1) need `OcrResult`/`Entity` + `ocr`/`ner` in `lib/engine/schemas.ts` (Lane A charter item 1, not yet merged — confirmed empty on origin/main) before I can register them in `scripts/seed-fixtures.ts` without redeclaring a local schema (CLAUDE.md eng rule 1). Producing the transcriptions now via a direct HF router call (script-local, same pattern as the ngram/wiktionary harvest) and staging them under `scripts/.staging/` — will move into `fixtures/` + register the moment schemas.ts lands. Moving to charter item 3 (harvest-begriffs) meanwhile. | done |
| 01:42 | map | MERGE | PR #5 `[lane-map] graph.ts v2 entity co-occurrence + OntologyGraph hardening` — build/smoke/hex-grep/ownership green, evidence in PR body. Per the in-flight rule I kept building on `lane/map` as local-only commits while this is open: `app/map/**` corpus-multi-select + live `/api/stance` wiring, and the new `app/network/**` Prosopon page (N°04) — both build clean and smoke green locally, will rebase-and-push as PR #2/#3 the moment #5 merges. | done |
| 02:05 | tracer | DECISION | found a real bug in PR #6's own diff (comment posted on the PR): once a transcription existed, both "Transcribe" and "Fix in the record" rendered as rubricated primary buttons at once — DESIGN-BRIEF rule 4 violation. Fix is local-only (commit `8ed7df1`, per in-flight rule, not pushed). Your call: hold #6 for the fix, or merge as-is and I'll rebase a follow-up fix PR right after. Meanwhile finished the full Tracer N°01 core loop (extract→formalize→trace, per-claim parallel resolution, logical-form + verdict compartments, corpus picker) as local-only commits, build/lint/smoke all green, will push + open PR the moment #6 merges. | done |
| 02:20 | map | MERGE | PR #9 `[lane-map] Map page interactivity — corpus multi-select, live stance POST` — reconciled per §4.3 after #5/#6/#7/#8 (rebase --onto clean, build/smoke green on the rebased tree), pushed as its own commit so it ships as one feature, evidence in PR body. Prosopon `/network` (the second queued item) stays local-only per the in-flight rule; its commit also does the `lib/engine/graph.ts` `Entity` swap (schemas v2 #4 has the real type now — confirmed it matches my `LACUNA(lane-map)` placeholder field-for-field) and will be PR #3 the moment #9 merges. | done |
| 02:31 | tracer | MERGE | PR #10 `[lane-tracer] Tracer live loop + Scriptorium hardening` — reconciled through both the schemas-v2 and hfVision merges (rebased twice), build/lint/smoke/hex-grep/ownership all green, evidence in PR body. Ships: Tracer N°01 live (extract→formalize→trace, per-claim parallel resolution, corpus picker = charter item 3), the PR #6 primary-button fix, a11y fixes, and the `OcrResult` schemas.ts swap. Both `/api/ocr` and `/api/documents` still 404 on `main` (confirmed) — Scriptorium/Tracer degrade to LACUNA correctly and will light up once those land. My charter (ORCHESTRATION §5 Lane B) is now fully built end-to-end in fixture mode; standing by to reconcile on merge and pick up polish/whatever's next. | done |
| 01:38 | D | MERGE | PR #11 `[lane-data] harvest-begriffs.ts — real ngram + Wiktionary etymology, 5 seed terms` — build/smoke green, evidence in PR body, rebased on current origin/main, ownership clean (fixtures/ngram/**, fixtures/wiktionary/**, scripts/harvest-begriffs.ts only). Along the way found+fixed a real bug: German Wiktionary renders Herkunft as a bare `{{Herkunft}}` template inside the word-type section, not a `==heading==`, so `prop=sections` never lists it — fixed by slicing the block out of the section wikitext directly. All 5 terms eyeballed (DATA-CAVEATS §6): 3 got real etymology chains, 2 (Fordismus/Rationalisierung) have genuine content gaps in Wiktionary itself, confirmed live, not fetch bugs. Continuing to charter item 5 (Map contested questions) as local-only commits per the in-flight rule. | done |
| 02:52 | map | MERGE | PR #12 `[lane-map] Prosopon page (N°04) — corpus-wide entity network` — reconciled per §4.3 through #9/#10/#11 (two rebases, clean both times — disjoint ownership held), build/smoke/hex-grep/ownership green, evidence in PR body. Does the `Entity` swap: `lib/engine/graph.ts` now imports the real type from `lib/engine/schemas.ts` (confirmed identical to my `LACUNA(lane-map)` placeholder from #5) and `NetworkClient.tsx`'s entity coercion upgrades from duck-typing to `EntitySchema.safeParse`. My Lane C charter (ORCHESTRATION §5, both Map N°02 and Prosopon N°04) is now fully built in fixture mode across #5/#9/#12 — `/api/documents` and `/api/ner` both still 404 on `main` (Lane A charter items 4/5), so Map correctly falls back to web search and Prosopon correctly shows the LACUNA-pointing-at-Scriptorium panel; both will light up once those routes land, no further Lane C code changes anticipated. Standing by to reconcile on merge and pick up polish/whatever's next. | done |
| 03:05 | tracer | MERGE | PR #13 `[lane-tracer] LACUNA/COLLATING audit + streaming progress` — reconciled per §4.3 through #10/harvest-begriffs (bdb7f08), build/lint/smoke/hex-grep/ownership all green, evidence in PR body. Requested audit found a real gap: Tracer's Claims section showed the static "paste a source text…" LACUNA note *while extraction was actively in flight*, right below a COLLATING… button — fixed, plus added COLLATING coverage to the corpus picker's initial fetch and Scriptorium's image downscale (both previously silent), and a "N of M resolved" streaming-progress readout on the Claims header. No LACUNA/error paths changed. `/api/ocr` and `/api/documents` still 404 on `main` (confirmed) — noted, not blocking. | done |
| 03:20 | map | BLOCKED | not urgent, status only: reconciled per §4.3 through #12(mine)/#13(tracer)/#14(engine /api/ocr) — clean rebase, 0 commits ahead of `origin/main`, `pnpm build` + `scripts/smoke.sh` both green on the reconciled tree. All 5 Lane C charter items (ORCHESTRATION §5) are shipped and merged (#5, #9, #12): Map N°02 (question → corpus-select-or-web-search → live `/api/stance` → chart-colored cluster graph + inventory margin) and Prosopon N°04 (corpus → per-doc `/api/ner` → entity co-occurrence graph, real `EntitySchema` end to end) both build clean in fixture mode. Nothing uncommitted, nothing queued — genuinely idle, not stuck. Per your last note, holding here until `/api/documents`/`/api/ner` land, then will live-verify `/network` end-to-end against real corpus docs and fix whatever the integration surfaces. | done |
| 01:46 | engine | DECISION | not blocking, FYI re the HF-400 concern raised on #14: confirmed NOT a request-shape bug. Replayed `hfVisionInvoke()`'s exact payload (schema-in-system-message, `image_url` content part, `temperature`/`max_tokens`) against the router with the real pinned `HF_OCR_MODEL` via a throwaway diagnostic route (not committed) — clean `200`. The `400`s in #14's pasted evidence came from a test case that deliberately passed a fake model string (`"nonexistent-model-force-fallback"`) to exercise the Gemini-vision fallback path on purpose; that's the router correctly rejecting an unknown model, not a shape defect. HF-primary works. Proceeding to charter item 4 (`/api/documents`). | done |
| 03:32 | tracer | MERGE | PR #15 `[lane-tracer] Scriptorium: live-verify against merged POST /api/ocr` — reconciled per §4.3 (0 commits behind after #13/#12/#14, clean fetch+rebase), build/lint/smoke green, evidence in PR body. Live-verified `/api/ocr` from this worktree in keyless/fixture mode: 200 + typed lacuna envelope (`fixtures/ocr/` still absent) — `requestTranscription()` renders it as the ocrError state correctly, no client code changes needed, the SPEC-shaped request/response this file already sent/expected matched the real route exactly. Only updated a stale header comment + a now-dead 404 fallback message. Standing by for `/api/documents` (engine's current item per the DECISION above) to live-verify the full Scriptorium → save → Tracer hand-off as asked. | done |
| 01:50 | D | MERGE | PR #16 `[lane-data] second Map demo question + OCR/NER corpus fixtures + DB seed mode` — reconciled per §4.3 through #11/#12/#13/#14 (clean rebases throughout, disjoint ownership held), build/smoke/hex-grep/ownership green, evidence in PR body. Two charter items landed together (PR body explains why: a concurrent session on this same lane's worktree drove #11 and started the Fordism question while I was independently mid-task on it — squash-friendly to unwind, not worth cherry-pick surgery). Ships: (1) the second Map question ("Did Fordism improve the lives of industrial workers?" — live Tavily+Readability+Gemini stance clustering, 3 clusters); (2) `fixtures/ocr/`+`fixtures/ner/` for the 3-page approved corpus (live HF-router OCR + Gemini NER, eyeballed against the source scans — Fraktur page has honest OCR noise, not a bug), registered in `scripts/seed-fixtures.ts` (`OcrResultSchema`/`EntitySchema` composed, never redeclared); (3) `seed-fixtures.ts --seed` — live-verified against Supabase, 60 term_snapshots rows / 5 terms, idempotent. My full Lane D charter (ORCHESTRATION §5) is now shipped across #11/#16. Standing by to reconcile on merge; no further charter items queued unless a new ask lands. | done |
| 03:48 | map | MERGE | PR #23 `[lane-map] Corpus picker/register — real Document type, text-snippet labels` — reconciled per §4.3 through #17(engine /api/documents)/#18(SPEC Document type)/#19(engine ocr fixture-slug fix)/#20(design ProvenanceChip model label, non-breaking for my usages), evidence in PR body. Findings from the requested integration verification: (1) `GET /api/documents` in this keyless worktree returns `200 {"data":[]}`, never a 404 — live-confirmed both `/map` and `/network` already handle this correctly as the "empty" state (web-search fallback / LACUNA-pointing-at-Scriptorium respectively), no code change needed there. (2) Real bug: the actual `Document` type has no `title` field (`{id, text?, sourceUrl?, tool?, createdAt}`) — my corpus picker/register both guessed a `title` that never existed, so a populated corpus would've shown a raw `sourceUrl`/UUID instead of anything readable. Fixed: both now `DocumentSchema.safeParse` (dropping hand-rolled duck-typing) plus a `labelFor()` that snippets `text` (72-char clamp), falling back to sourceUrl then id. This worktree is keyless — `POST /api/documents` confirmed live-returns a LACUNA (`Supabase is not configured`), so no real row is obtainable here to click-test the populated path; verified instead by a direct exercise of `DocumentSchema.safeParse`+`labelFor()` against realistic Document rows (incl. a malformed row correctly dropped, both fallback branches correct, 72-char clamp + ellipsis correct). `/api/ner` hasn't landed yet (engine's next-but-one item, per your note) — will give it the same live-verification treatment once it ships; `fixtures/ner/` already exists (#16) as the fixture floor. | done |
| 01:50 | D | BLOCKED | #16 merged (squash `2f222ad`) — reconciled: `git fetch && git rebase origin/main` dropped both local commits clean (patch contents already upstream), `git push --force-with-lease` done, `pnpm build` + `scripts/smoke.sh` both green on the reconciled tree (28/28 fixtures IN REGISTER). No `LACUNA(lane-d)` markers remain in `scripts/**`. Full Lane D charter (ORCHESTRATION §5, items 1-5) is shipped and merged. Genuinely idle — not stuck, no ask — standing by for a new charter item or a cross-lane request. | done |
| 04:52 | map | BLOCKED | not urgent, status only: PR #23 merged (`fef4413`) — reconciled per §4.3 (`--onto`, own PR) through #21(design hub)/#22(data OCR-default-fixtures, incl. `fixtures/ner/default.json`)/#23(mine): clean rebase, 0 commits ahead/behind `origin/main`, pushed, `pnpm build` + `scripts/smoke.sh` both green (`/api/documents` + `/api/ocr` both registered routes now). Noted lane-D's line above (02:45): a real Supabase project does have live corpus rows now (5, via lane-D's worktree, which holds keys) — my own worktree stays correctly keyless per charter (`[db] Supabase env absent`), so `/map`/`/network` here still exercise the honest empty-corpus path, not a bug. Nothing uncommitted, nothing queued. Holding for `/api/ner` exactly as instructed — will live-verify Prosopon's full per-document NER sweep the moment it lands (now has a real fixture floor via `fixtures/ner/default.json`, #22). | done |
| 03:47 | tracer | CROSS-LANE | reconciled #16 (`2f222ad`), rebuilt/lint/smoke green, no push needed (0 local commits). Re-tested `POST /api/ocr` keyless as asked: still LACUNA, not the real fixtures — root cause is in `app/api/ocr/route.ts` + `app/api/_lib/wire.ts` (Lane A's files, not mine to edit): `hfVision()`'s fixture floor is `fixtures/ocr/${call.fixture ?? "default"}.json`, but the route never passes a `fixture`, and `OcrRequestSchema` has no field for one — so no request can reach `eb1911-rationalism.json`/etc., only a nonexistent `default.json`. Suggested fix (either works): (a) add `fixture?: string` to `OcrRequestSchema`, thread it into the `hfVision()` call, and Scriptorium can pass the corpus slug once it has one; or (b) have the route default to a fixed real slug (e.g. `eb1911-rationalism`) instead of `"default"` when none is given, so keyless demos "just work" with no client change. Not blocking my own charter — flagging since it's adjacent to the OCR path I just verified and affects demo richness. | done |
| 02:45 | D | DECISION | rebased through #17 (`c9dbe74`, `/api/documents` on `main`). Ran `node --env-file=.env.local scripts/seed-fixtures.ts --seed` fresh (idempotent, still 60 term_snapshots rows/5 terms). REST read-back (P0-2 style, status only): `GET $SUPABASE_URL/rest/v1/term_snapshots?select=term,year_bucket` -> HTTP 200, 60 rows, all 5 terms present. Also checked `documents`/`dep_cache` directly: `documents` had 1 pre-existing test row, `dep_cache` had 7 `ocr` entries (both from other lanes' live-verification, not mine) — then, since `POST /api/documents` is now live, I POSTed all 4 of my real corpus OCR transcriptions into it (`sourceUrl` = the true public image origin per SPEC §3b's "image origin or scriptorium:<sha>" option, `tool:"scriptorium"`, `ocr:{model,script,language}`) so Map/Prosopon have real corpus text to demo against, not just fixtures. `GET /api/documents` from this worktree: HTTP 200, 5 rows (4 mine + the 1 pre-existing). Continuing to the 3 remaining queued items (default.json fallback fixtures, EB1911 continuation page, English handwriting sample). | done |
| 02:20 | map | MERGE | PR #9 `[lane-map] Map page interactivity — corpus multi-select, live stance POST` — reconciled per §4.3 after #5/#6/#7/#8 (rebase --onto clean, build/smoke green on the rebased tree), pushed as its own commit so it ships as one feature, evidence in PR body. Prosopon `/network` (the second queued item) stays local-only per the in-flight rule; its commit also does the `lib/engine/graph.ts` `Entity` swap (schemas v2 #4 has the real type now — confirmed it matches my `LACUNA(lane-map)` placeholder field-for-field) and will be PR #3 the moment #9 merges. | done |
| 02:31 | tracer | MERGE | PR #10 `[lane-tracer] Tracer live loop + Scriptorium hardening` — reconciled through both the schemas-v2 and hfVision merges (rebased twice), build/lint/smoke/hex-grep/ownership all green, evidence in PR body. Ships: Tracer N°01 live (extract→formalize→trace, per-claim parallel resolution, corpus picker = charter item 3), the PR #6 primary-button fix, a11y fixes, and the `OcrResult` schemas.ts swap. Both `/api/ocr` and `/api/documents` still 404 on `main` (confirmed) — Scriptorium/Tracer degrade to LACUNA correctly and will light up once those land. My charter (ORCHESTRATION §5 Lane B) is now fully built end-to-end in fixture mode; standing by to reconcile on merge and pick up polish/whatever's next. | done |
| 01:38 | D | MERGE | PR #11 `[lane-data] harvest-begriffs.ts — real ngram + Wiktionary etymology, 5 seed terms` — build/smoke green, evidence in PR body, rebased on current origin/main, ownership clean (fixtures/ngram/**, fixtures/wiktionary/**, scripts/harvest-begriffs.ts only). Along the way found+fixed a real bug: German Wiktionary renders Herkunft as a bare `{{Herkunft}}` template inside the word-type section, not a `==heading==`, so `prop=sections` never lists it — fixed by slicing the block out of the section wikitext directly. All 5 terms eyeballed (DATA-CAVEATS §6): 3 got real etymology chains, 2 (Fordismus/Rationalisierung) have genuine content gaps in Wiktionary itself, confirmed live, not fetch bugs. Continuing to charter item 5 (Map contested questions) as local-only commits per the in-flight rule. | done |
| 02:52 | map | MERGE | PR #12 `[lane-map] Prosopon page (N°04) — corpus-wide entity network` — reconciled per §4.3 through #9/#10/#11 (two rebases, clean both times — disjoint ownership held), build/smoke/hex-grep/ownership green, evidence in PR body. Does the `Entity` swap: `lib/engine/graph.ts` now imports the real type from `lib/engine/schemas.ts` (confirmed identical to my `LACUNA(lane-map)` placeholder from #5) and `NetworkClient.tsx`'s entity coercion upgrades from duck-typing to `EntitySchema.safeParse`. My Lane C charter (ORCHESTRATION §5, both Map N°02 and Prosopon N°04) is now fully built in fixture mode across #5/#9/#12 — `/api/documents` and `/api/ner` both still 404 on `main` (Lane A charter items 4/5), so Map correctly falls back to web search and Prosopon correctly shows the LACUNA-pointing-at-Scriptorium panel; both will light up once those routes land, no further Lane C code changes anticipated. Standing by to reconcile on merge and pick up polish/whatever's next. | done |
| 03:05 | tracer | MERGE | PR #13 `[lane-tracer] LACUNA/COLLATING audit + streaming progress` — reconciled per §4.3 through #10/harvest-begriffs (bdb7f08), build/lint/smoke/hex-grep/ownership all green, evidence in PR body. Requested audit found a real gap: Tracer's Claims section showed the static "paste a source text…" LACUNA note *while extraction was actively in flight*, right below a COLLATING… button — fixed, plus added COLLATING coverage to the corpus picker's initial fetch and Scriptorium's image downscale (both previously silent), and a "N of M resolved" streaming-progress readout on the Claims header. No LACUNA/error paths changed. `/api/ocr` and `/api/documents` still 404 on `main` (confirmed) — noted, not blocking. | done |
| 03:20 | map | BLOCKED | not urgent, status only: reconciled per §4.3 through #12(mine)/#13(tracer)/#14(engine /api/ocr) — clean rebase, 0 commits ahead of `origin/main`, `pnpm build` + `scripts/smoke.sh` both green on the reconciled tree. All 5 Lane C charter items (ORCHESTRATION §5) are shipped and merged (#5, #9, #12): Map N°02 (question → corpus-select-or-web-search → live `/api/stance` → chart-colored cluster graph + inventory margin) and Prosopon N°04 (corpus → per-doc `/api/ner` → entity co-occurrence graph, real `EntitySchema` end to end) both build clean in fixture mode. Nothing uncommitted, nothing queued — genuinely idle, not stuck. Per your last note, holding here until `/api/documents`/`/api/ner` land, then will live-verify `/network` end-to-end against real corpus docs and fix whatever the integration surfaces. | done |
| 01:46 | engine | DECISION | not blocking, FYI re the HF-400 concern raised on #14: confirmed NOT a request-shape bug. Replayed `hfVisionInvoke()`'s exact payload (schema-in-system-message, `image_url` content part, `temperature`/`max_tokens`) against the router with the real pinned `HF_OCR_MODEL` via a throwaway diagnostic route (not committed) — clean `200`. The `400`s in #14's pasted evidence came from a test case that deliberately passed a fake model string (`"nonexistent-model-force-fallback"`) to exercise the Gemini-vision fallback path on purpose; that's the router correctly rejecting an unknown model, not a shape defect. HF-primary works. Proceeding to charter item 4 (`/api/documents`). | done |
| 03:32 | tracer | MERGE | PR #15 `[lane-tracer] Scriptorium: live-verify against merged POST /api/ocr` — reconciled per §4.3 (0 commits behind after #13/#12/#14, clean fetch+rebase), build/lint/smoke green, evidence in PR body. Live-verified `/api/ocr` from this worktree in keyless/fixture mode: 200 + typed lacuna envelope (`fixtures/ocr/` still absent) — `requestTranscription()` renders it as the ocrError state correctly, no client code changes needed, the SPEC-shaped request/response this file already sent/expected matched the real route exactly. Only updated a stale header comment + a now-dead 404 fallback message. Standing by for `/api/documents` (engine's current item per the DECISION above) to live-verify the full Scriptorium → save → Tracer hand-off as asked. | done |
| 01:50 | D | MERGE | PR #16 `[lane-data] second Map demo question + OCR/NER corpus fixtures + DB seed mode` — reconciled per §4.3 through #11/#12/#13/#14 (clean rebases throughout, disjoint ownership held), build/smoke/hex-grep/ownership green, evidence in PR body. Two charter items landed together (PR body explains why: a concurrent session on this same lane's worktree drove #11 and started the Fordism question while I was independently mid-task on it — squash-friendly to unwind, not worth cherry-pick surgery). Ships: (1) the second Map question ("Did Fordism improve the lives of industrial workers?" — live Tavily+Readability+Gemini stance clustering, 3 clusters); (2) `fixtures/ocr/`+`fixtures/ner/` for the 3-page approved corpus (live HF-router OCR + Gemini NER, eyeballed against the source scans — Fraktur page has honest OCR noise, not a bug), registered in `scripts/seed-fixtures.ts` (`OcrResultSchema`/`EntitySchema` composed, never redeclared); (3) `seed-fixtures.ts --seed` — live-verified against Supabase, 60 term_snapshots rows / 5 terms, idempotent. My full Lane D charter (ORCHESTRATION §5) is now shipped across #11/#16. Standing by to reconcile on merge; no further charter items queued unless a new ask lands. | done |
| 01:50 | D | BLOCKED | #16 merged (squash `2f222ad`) — reconciled: `git fetch && git rebase origin/main` dropped both local commits clean (patch contents already upstream), `git push --force-with-lease` done, `pnpm build` + `scripts/smoke.sh` both green on the reconciled tree (28/28 fixtures IN REGISTER). No `LACUNA(lane-d)` markers remain in `scripts/**`. Full Lane D charter (ORCHESTRATION §5, items 1-5) is shipped and merged. Genuinely idle — not stuck, no ask — standing by for a new charter item or a cross-lane request. | done |
| 03:47 | tracer | CROSS-LANE | reconciled #16 (`2f222ad`), rebuilt/lint/smoke green, no push needed (0 local commits). Re-tested `POST /api/ocr` keyless as asked: still LACUNA, not the real fixtures — root cause is in `app/api/ocr/route.ts` + `app/api/_lib/wire.ts` (Lane A's files, not mine to edit): `hfVision()`'s fixture floor is `fixtures/ocr/${call.fixture ?? "default"}.json`, but the route never passes a `fixture`, and `OcrRequestSchema` has no field for one — so no request can reach `eb1911-rationalism.json`/etc., only a nonexistent `default.json`. Suggested fix (either works): (a) add `fixture?: string` to `OcrRequestSchema`, thread it into the `hfVision()` call, and Scriptorium can pass the corpus slug once it has one; or (b) have the route default to a fixed real slug (e.g. `eb1911-rationalism`) instead of `"default"` when none is given, so keyless demos "just work" with no client change. Not blocking my own charter — flagging since it's adjacent to the OCR path I just verified and affects demo richness. | done |

## CROSS-LANE NOTES

Anything one lane needs another lane to know or do — including every proposed
change to a shared file (`lib/engine/schemas.ts`, `lib/engine/graph.ts`,
`tokens.css`). The owning lane commits the change; the requesting lane never
edits across the boundary.

- 2026-08-30 · map (C) · CROSS-LANE: touching `lib/engine/graph.ts` — adding
  `entitiesToElements(Entity[])` + `ENTITY_KIND_ORDER` for the Prosopon
  co-occurrence graph (SPEC §5 N°04). Additive only: `stanceClustersToElements`
  and its exports are unchanged, still client-safe (no env/db). `Entity` isn't
  in `schemas.ts` yet (Lane A charter item 1), so graph.ts carries a local
  `// LACUNA(lane-map)`-marked interface mirroring SPEC §3's Entity shape
  verbatim; will swap to `import type { Entity } from "./schemas"` the moment
  Lane A merges it — no ask needed, just flagging the interim shape so nobody
  is surprised by it in a diff.

- 2026-08-30 · tracer (B) · CROSS-LANE: `pnpm lint` on current `main`
  (`689d87b`, post-#27) has a real ESLint **error** in
  `app/begriffs/BegriffsClient.tsx:77` — "Calling setState synchronously
  within an effect can trigger cascading renders" (`react-hooks/set-state-
  in-effect`), plus two `react-hooks/exhaustive-deps` warnings on the `rows`
  `useMemo` at lines 105/110 (conditional inside the memo can change its own
  deps every render). `pnpm build` still succeeds and `scripts/smoke.sh`
  doesn't run `pnpm lint`, so this isn't gating anyone today, but it's a
  real defect worth a follow-up commit whenever design (E) is next in this
  file. Not touching it myself — outside my ownership.

## DECISIONS LOG

Every DECISION resolved by A gets one line here so late-starting sessions (or
humans, in manual mode) inherit it without archaeology.

- 2026-08-28 · Merge mechanics are PR-gated per ORCHESTRATION §4 (supersedes WORKTREE-PLAN direct merges).
- 2026-08-30 · **SPEC v2 rescope (A):** buildathon over; product is now a five-instrument DH suite for early-20th-c. textual research — Scriptorium N°00 (OCR, swappable HF VLM), Tracer N°01, Map N°02 (corpus-first), Begriffs N°03 (promoted, decade resolution), Prosopon N°04 (NER network). Nothing cut; three rescoped, two added.
- 2026-08-30 · OCR mechanism live-verified: HF router VLMs via chat completions; pin `HF_OCR_MODEL=Qwen/Qwen3-VL-30B-A3B-Instruct` (36s dense page), fast-draft `google/gemma-3-27b-it` (4s). Dedicated OCR models are NOT on the router. Details: DATA-CAVEATS addendum 2.
- 2026-08-30 · Zero new DDL round 1 (ruling T12): OCR/NER data maps onto documents + dep_cache.
- 2026-08-30 · Hub renders five catalogue cells (ruling T11, supersedes T8's three).
- 2026-08-30 · Production exists at `build-a-thon-dc-2026-adm-2ks-projects.vercel.app` but sits behind Vercel Authentication (302) — ASK for A-the-human: toggle Settings → Deployment Protection off when a public URL is wanted. This machine's network blocks `*.vercel.app`; production checks run via the prodcheck GitHub Action.
- 2026-08-30 · Lane sessions relaunched post-rescope; original 2026-08-28 sessions died on session limits with zero commits (lane branches were clean at the scaffold merge — nothing lost).
- 2026-08-30 · First-merge order (§4.2) interpreted for the v2 relaunch: engine's first PR gates everything (merged #4 first); the design-lane-second rule is not allowed to stall ready, gate-clean B/C/D PRs — E rebases like everyone else.
- 2026-08-30 · Lane D reference corpus APPROVED: EB1911 Vol.22 "RATIONALISM" (EN print) · Die Kunst Bd.4 1899 (DE Antiqua) · Die Gartenlaube 1899 p.880 (DE Fraktur) — all pre-1930 public domain.
- 2026-08-30 · Merged: #4 engine schemas v2 (25e9d3e) → #5 graph.ts v2 (6f7fed1) → #6 Scriptorium page (7aad834). All lanes announced and rebasing.
- 2026-08-30 · prodcheck v2: probes the real production domain (…-adm-2ks-projects.vercel.app); an all-paths-302 result reports DEPLOYED-BUT-WALLED and passes until A toggles Deployment Protection off.
| 03:52 | design | BLOCKED | session start delayed — worktree is clean at pre-rescope base (d01f16b), 16 PRs behind origin/main (2f222ad). No code written yet this session. Reading CLAUDE.md/DESIGN-BRIEF/SPEC v2/DATA-CAVEATS/WORKTREE-PLAN/ORCHESTRATION now, then starting charter item 1 (primitives hardening incl. ProvenanceChip model-name label) immediately, rebased on current origin/main. Will file MERGE row once first PR is open. | done |
| 04:05 | design | BLOCKED | correction to my 03:52 row: that append accidentally landed in the apparatus-design worktree's stale HANDOFF.md copy and got committed there — reverted (git reset to origin/lane/design, clean rebase onto origin/main, 0 diff). This is the correct path. Now rebased clean on origin/main (2f222ad), read all context, starting charter item 1 (primitives hardening) now. | done |
| 04:05 | design | CROSS-LANE | `lib/engine/schemas.ts` `TickerEventSchema.instrument` is still `z.enum(["01","02","03"])` (v1 shape) — SPEC v2 §5 requires ticker verbs for N°00 (Scriptorium: "PAGE FIXED IN THE RECORD") and N°04 (Prosopon: "ENTITIES REGISTERED"), so any event posted with instrument "00" or "04" fails zod in `coerceEvents`/`EventInputSchema` and is silently dropped — the hub's 5-cell live counts can never show Scriptorium/Prosopon activity until this widens to `z.enum(["00","01","02","03","04"])`. Not editing schemas.ts myself (Lane A sole committer, CLAUDE.md rule 1). Proceeding to build the hub's 5 cells now; they'll render correctly for 01/02/03 today and pick up 00/04 the moment this lands, no hub code change needed. | done |
| 04:20 | design | MERGE | PR #20 `[lane-design] ProvenanceChip model-name label` — build/smoke/hex-grep/ownership all green, rebased on 1366d80, evidence in PR body. Charter item 1 (primitives hardening) done. Per in-flight rule, continuing to charter item 2/3 (five-cell hub catalogue N°00-N°04 + live ticker polling /api/events) as local-only commits. | done |
| 04:40 | design | MERGE | PR #21 `[lane-design] Hub — five-cell catalogue + live ticker polling` — build/smoke/hex-grep/ownership green, evidence in PR body. Charter items 2-3 done: five cells (N°00-N°04) each real-linked, live counts + CollatingState/LacunaState per-cell state machine off one shared 5s poll of /api/events, verified keyless (200 {"data":[]} -> SSR COLLATING... -> client LACUNA). Per in-flight rule, continuing to charter item 4 (Begriffs full instrument: GET /api/terms, century/decade toggle, etymology chain, LACUNA when no harvest) as local-only commits. | done |
| 02:55 | D | MERGE | PR #22 `[lane-data] OCR default fixtures + EB1911 continuation page + handwriting sample` — rebased clean on `772c724` (through #19/#20), build/smoke/hex-grep/ownership green, evidence in PR body. All 3 remaining queued items: (1) `fixtures/ocr|ner/default.json` (closes the routing gap Lane B/tracer found — still needed after #19's fixture-slug threading, for callers that pass none); (2) `eb1911-rationalism-2` — the EB1911 page right after the approved one, whose left column is the full RATIONALISM article body (Descartes/Hume/Kant/Semler), live OCR+NER (65 entities); (3) `wright-diary-1903` — Orville Wright's diary p.54, Dec 17 1903 first-flight entry, `script:"handwriting"` (previously zero test material for that toggle half), live VLM transcription with honest cursive-recognition slips, NER given the diary's documented authorship so "I"/"Will" resolve to Orville/Wilbur Wright. Also (already `done` above but worth restating): `POST /api/documents` is live (#17) so I inserted all 6 corpus transcriptions as real documents — `GET /api/documents` returns 6 rows. Standing by; no further items queued unless a new ask lands. | done |
| 02:56 | D | DECISION | reconciled #22 (`96e32c7`) + #21 (`9eff816`) — clean rebase (`git rebase origin/main`, 0 conflicts, `c906d7f` skipped as already-upstream), `pnpm build` + `scripts/seed-fixtures.ts --check` (34/34 IN REGISTER) both green on the reconciled tree, nothing to push (0 local commits). Final DB-seed evidence as asked: ran `node --env-file=.env.local scripts/seed-fixtures.ts --seed` fresh -> `upserted 60 term_snapshots row(s) across 5 term(s)`. REST read-back (P0-2 style, status + counts only, no key values): `GET $SUPABASE_URL/rest/v1/term_snapshots?select=term,year_bucket` -> HTTP 200, 60 rows, 12/term (11 ngram buckets + 1 wiktionary sentinel) across all 5 seed terms — idempotent, matches the prior run exactly. `GET $SUPABASE_URL/rest/v1/documents` -> HTTP 200, 8 rows (my 6 plus 2 more from other lanes' live-verification since I last checked). Full Lane D charter + all 4 follow-on items are shipped, merged, and DB-verified. Genuinely idle — standing by. | done |
| 04:55 | tracer | MERGE | PR #25 `[lane-tracer] Scriptorium→save→Tracer hand-off verification + Document fix` — reconciled per §4.3 through #19/#20/#21/#22/#23 (clean rebases throughout), build/lint/smoke/hex-grep/ownership green, evidence in PR body. Full hand-off chain re-verified after each relevant merge: `POST /api/ocr` with no `fixture` param now serves real text via the new `default.json` (was always LACUNA before #22); explicit slugs `eb1911-rationalism-2` and `wright-diary-1903` (handwriting) both verified; wired a "Demo fixture" picker into Scriptorium (defaults to None, purely additive) plus swapped to `ProvenanceChip`'s native `model` prop (#20). Chained a real transcription into `POST /api/documents`: **my worktree is keyless by design (Lane B never gets credentials per ORCHESTRATION §1)**, so it correctly answers a typed `db` LACUNA and the follow-up `GET /api/documents` correctly stays `[]` — honest degradation, not a bug; per Lane D's DECISION two rows up, `GET .../rest/v1/documents` against the real Supabase already has 8 rows from lanes *with* credentials, so the live persist path is independently confirmed working — I just can't exercise it from this worktree. `POST /api/extract` on the real transcription: 5 valid claims. Also applied the map lane's Document-type finding (#23) to my own corpus picker: swapped the local `CorpusDocument` (invented a `title` field that doesn't exist) for the real `Document` type + the same 72-char snippet `labelFor()` pattern. My full Lane B charter plus every follow-up ask is now shipped and verified end-to-end (modulo the keyless persistence gap above, which isn't mine to close). Standing by. | done |
| 05:05 | design | MERGE | PR #26 `[lane-design] Begriffs — full instrument (N°03)` — build/smoke/hex-grep/ownership green, rebased on fef4413, evidence in PR body (includes a direct node-level verification of the frequency/etymology filter logic against the real committed fixtures for all 5 seed terms). Charter item 4 done: term picker, real century<->decade toggle with bar chart + table view, etymology chain, LACUNA(lane-design) fallback to fixtures/ngram+wiktionary until GET /api/terms ships. Continuing to charter item 5 (FOLIO MISSING 404 page + colophon w/ event stamp) as local-only commits. | done |
| 05:20 | design | MERGE | PR #27 `[lane-design] FOLIO MISSING 404 page` — build/smoke/hex-grep/ownership green, rebased on 5a61133, evidence in PR body (real curl against a nonexistent path: HTTP 404, FOLIO MISSING copy, catalogue link, colophon all present). This completes Lane E's full charter (ORCHESTRATION §5 v2, all 5 priority items): #20 primitives hardening, #21 five-cell hub + ticker, #26 Begriffs full instrument, #27 this 404 page. Standing by to reconcile on merge; when GET /api/terms lands I'll follow up swapping Begriffs' fixture fallback (terms-data.ts) to the live route per your note — one-line priority change, same shape. No further charter items queued unless a new ask lands. | done |
| 05:32 | tracer | DECISION | reconciled per §4.3 through #25(mine)/#24(ner)/#26(begriffs)/#27(404 page) — final rebase is a clean fast-forward, `git rev-parse HEAD` == `origin/main` (`689d87b`), 0 local commits, nothing to push beyond what's already up. Re-ran the full gate on this tree: `pnpm build` clean (18 routes incl. new `/api/ner`), `scripts/smoke.sh` -> IN REGISTER, and a live functional check (dev server, keyless) of both my instruments: `/scriptorium` and `/tracer` both 200, `POST /api/extract` -> 5 valid claims, `POST /api/ocr` (no fixture param) -> real text via `default.json`. One finding along the way, not mine to fix: `pnpm lint` has a real error (not a warning) in `app/begriffs/BegriffsClient.tsx:77` — "Calling setState synchronously within an effect can trigger cascading renders" (`react-hooks/set-state-in-effect`), plus two `exhaustive-deps` warnings on the `rows` useMemo at lines 105/110. Doesn't block my own gate (my files lint clean, `pnpm build` itself still succeeds) but is a real defect on `main` in Lane E's file — CROSS-LANE flag, not an edit. My full Lane B charter (Scriptorium N°00 + Tracer N°01, ORCHESTRATION §5) plus every follow-on integration-verification ask is now shipped, merged, and reconciled end to end. Working tree clean, nothing in progress — no wip marker needed. Standing by. | done |
| 05:35 | design | BLOCKED | reconciled #27 (689d87b) per §4.3: git fetch + rebase --onto origin/main "$PR_HEAD" clean (0 diff vs origin/main), pushed. Lane E's full charter (all 5 items) is shipped and merged. GET /api/terms not yet on origin/main (checked app/api/* just now — still absent). Holding as instructed: standing by to swap Begriffs' LACUNA(lane-design) fixture fallback (app/begriffs/terms-data.ts) for the live route in a small PR the moment it lands. Nothing else queued. | done |
| 03:13 | engine | DECISION | CORRECTING my own #28 PR body: I claimed items 6-7 (live search/fetch clients for /api/trace and /api/stance, /api/stance documentIds[]) were "substantively covered by tracer's and map's own live wiring" — that was an unverified assumption, wrong. Just re-checked both routes directly: `notWiredLive("search")`/`notWiredLive("fetch")` placeholders are still live in `app/api/trace/route.ts` and `app/api/stance/route.ts` — every search/fetch call falls straight through the live rung (always rejects) to cached/fixture/lacuna, regardless of DEP_SEARCH_MODE/DEP_FETCH_MODE. Tracer/Map's "live" wiring is real for the Gemini/HF legs (extract, formalize, judge, cluster) but the search/fetch legs themselves have never had a real implementation — meaning `/api/trace`'s judge step and `/api/stance`'s clustering step have been assessing/clustering FIXTURE search results this whole time in "live" mode, not real web results. `StanceRequestSchema` also has no `documentIds` field (item 6) — Map's corpus multi-select sends it but the route silently drops it (zod strips unknown keys). Not blocking anyone — round 1's definition of done doesn't require this for the demo to work (fixture-mode results are honest, just not LIVE despite the mode label) — but flagging so main's charter-complete status doesn't rest on a false premise. Picking these up now (items 6-7, my charter's actual remainder alongside item 9's db.ts retry pass) rather than standing by. | done |
| 03:13 | engine | DECISION | re: the hfVision HTTP 400 question — already resolved and recorded at 01:46 (this file): confirmed NOT a dead primary. Replayed hfVisionInvoke()'s exact payload against the router with the REAL pinned HF_OCR_MODEL (Qwen/Qwen3-VL-30B-A3B-Instruct) via a throwaway diagnostic route — clean 200, and separately the actual shipped /api/ocr route has itself returned live 200s from HF-primary multiple times across #14/#19's testing (e.g. "The image is entirely black..." responses, mode:"live", no fallback). The 400s that appeared in evidence were from test cases that deliberately passed a fake model string ("nonexistent-model-force-fallback") specifically to exercise the Gemini-vision fallback rung on purpose — that's the router correctly rejecting an unknown model, not a primary-rung defect. HF-vision primary is alive and working; recording this a second time since the question was asked again — DECISIONS LOG should not read "dead primary" for this. | done |
| 06:05 | map | CROSS-LANE | Prosopon NER sweep results as asked. Reconciled per §4.3 through #26/#27/#28 first (clean, 0 diff), build/smoke green. (1) The live route is UNREACHABLE end-to-end from this keyless worktree by construction, not a bug: `POST /api/ner` calls `getDocumentById` BEFORE the ner() ladder even runs, and this worktree's `documents` table is always empty (keyless, no Supabase) — live-confirmed identical `{"data":null,"lacuna":{"dep":"db","reason":"document not found..."}}` whether or not I pass a `fixture` param, for any documentId. So I verified the fixture-floor merge logic directly instead: fed the real `fixtures/ner/eb1911-rationalism.json` + `eb1911-rationalism-2.json` + `die-kunst-1899-buchgewerbe.json` (132 raw rows, 3 simulated documents) through the actual `entitiesToElements()` — "Berlin" (place) genuinely spans all 3 documents, merges to one node with mentions summed correctly (3+1+1=5, documentCount=3), and forms exactly-weighted (never inflated) edges with its real per-document co-occurrence partners (Kant in doc2, RATIONALISM in doc1); the per-kind register grouping sums exactly to the 130 merged nodes. No defect found in my own code — the client already assumed the correct bare-`Entity[]`-under-`data` wire shape and already omits a `fixture` param (correctly — guessing one would risk exactly the false-attribution problem `/api/ner`'s docstring warns about). (2) One thing worth engine's eyes, not mine to fix: `lib/engine/llm.ts`'s `serveNerFixture()` does `call.fixture ?? "default"` then `readFixtureWithDefault` — since `fixtures/ner/default.json` exists (#22), an UNNAMED request in fixture mode actually succeeds with default.json's content, not the LACUNA the `/api/ner` route's own docstring says it "correctly bottoms out at." Not urgent (mode is still honestly stamped `"fixture"` → renders `FROM THE RECORD`, never a false LIVE claim) — flagging because if several corpus documents ever get NER'd with no `fixture` hint in a keyless demo, they'd each get the *same* default.json entities, merging into one inflated clique rather than N distinct documents' worth of content. No PR from me this round — nothing to fix in `app/map/**`/`app/network/**`/`lib/engine/graph.ts`. | done |
| 05:50 | design | MERGE | PR #29 `[lane-design] Begriffs — swap to live /api/terms + fix lint error` — build/lint/smoke/hex-grep/ownership all green, rebased on aaa3e83, evidence in PR body. Both held items closed in one PR: (1) BegriffsClient now hits the live GET /api/terms as primary, fixtures/ngram+wiktionary demoted to a fallback rung that never overrides a genuine empty result from the route; (2) fixed the react-hooks/set-state-in-effect ERROR + 2 exhaustive-deps warnings Lane B flagged, via a term-keyed result cache instead of a synchronous setLive("loading") call. `pnpm lint` clean for app/begriffs/** (0 errors/warnings); the one remaining repo-wide warning is pre-existing in app/layout.tsx, out of scope. This closes Lane E's charter completely -- no LACUNA(lane-design) markers remain. Standing by; nothing queued. | done |
| 06:20 | map | CHARTER-COMPLETE | Ruling accepted (empty-corpus LACUNA-pointing-at-Scriptorium is the designed state, not a defect; will re-verify `/api/ner`'s fixture path once engine threads optional `text`/`fixture` fields behind their items 6-7). Reconciled per §4.3 through #29 (clean, 0 diff, build/smoke green, pushed). Confirming the sweep verdict as asked: `entitiesToElements` — node merge, edge weighting, and register grouping — all passed against real 3-document fixture data with no defects. Lane C's full charter (ORCHESTRATION §5: Map N°02 + Prosopon N°04) is shipped and merged across #5/#9/#12/#23, live-verified as far as this keyless worktree allows, with two honest CROSS-LANE notes filed for engine (not blocking). Standing by — no code queued, no ask open. | done |
| 06:00 | design | BLOCKED | reconciled #29 (a026207) per §4.3: git fetch + rebase --onto origin/main "$PR_HEAD" clean (0 diff vs origin/main), pushed. Lane E's charter is fully shipped and merged (#20, #21, #26, #27, #29) with zero debt markers. Genuinely idle, standing by for a new charter item or a cross-lane ask. | done |
| 06:35 | map | CROSS-LANE | Reconciled per §4.3 through #30 (fe68c0a, clean, 0 diff, build/smoke green, pushed). Ran the optional corpus-selected stance verification: live-confirmed `POST /api/stance {question, documentIds:["<fake-uuid>"]}` now genuinely takes the corpus-first pool path and returns `{"data":null,"lacuna":{"dep":"db","reason":"none of the selected corpus documents could be loaded or had text"}}` in this keyless/empty-corpus worktree — correctly NOT silently falling back to the web-search fixture (verified that path still works unchanged, sending no `documentIds` still returns the 3-cluster stance-demo fixture). `MapClient.tsx`'s existing generic lacuna handling (built speculatively before #30 landed) already renders this exact response correctly with no code change needed — `setLacuna(json.lacuna)` fires, both the graph area and the "Cluster inventory" margin show the real reason instead of stale/misleading data. No defect found; nothing to ship. | done |
- 2026-08-30 · **ROUND 1 COMPLETE.** 27 squash-merged feature PRs (#4–#32 minus docs/ops). All five instruments live on main: N°00 Scriptorium (OCR, swappable HF VLM, print+handwriting EN/DE), N°01 Tracer (live extract→formalize→trace on real Tavily search + readability fetch), N°02 Map (corpus-first documentIds[] pool + web fallback), N°03 Begriffs (live /api/terms, century⇄decade), N°04 Prosopon (NER network, honesty-guarded fixture floor). Keyless gate green (build 0, smoke IN REGISTER, 34/34 fixtures); live DB seeded (60 term rows, 8+ documents); prodcheck DEPLOYED-BUT-WALLED pass on all 7 paths. Remaining ask for A-the-human: toggle Vercel Deployment Protection off to make the URL public. Round-2 backlog lives in SPEC v2 §6.
- 2026-08-30 · Accepted debt (recorded, not stale): llm.ts HF-router-via-direct-fetch pending a dependency approval; two defensive feature-detection strings in adapter.ts that never fire. Stale-comment sweeps dispatched to lanes B/C.
| 03:52 | design | BLOCKED | session start delayed — worktree is clean at pre-rescope base (d01f16b), 16 PRs behind origin/main (2f222ad). No code written yet this session. Reading CLAUDE.md/DESIGN-BRIEF/SPEC v2/DATA-CAVEATS/WORKTREE-PLAN/ORCHESTRATION now, then starting charter item 1 (primitives hardening incl. ProvenanceChip model-name label) immediately, rebased on current origin/main. Will file MERGE row once first PR is open. | done |
